#version 430 core

layout (binding = 0, rgba32f) uniform image1D destTex;
layout (binding = 1, rgba32f) uniform image1D srcTex;
layout(local_size_x = 1000) in;

void main(){
  int idx = int(gl_GlobalInvocationID.x);
  vec4 tmp = imageLoad(srcTex, idx);
  imageStore(destTex, idx, vec4(1,1,1,1));
}
